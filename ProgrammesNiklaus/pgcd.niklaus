// La compilation devrait reussir.
// Le programme demande a l'utilisateur de taper deux nombres,
// et affiche leur PGCD.

program PGCD;
var a, b;
{
	read a;
	read b;
	while(a <> b) {
		if(a > b) {
			a := a - b;
		} else {
			b := b - a;
		}
	}
	write a;
}